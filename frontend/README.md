# frontend

this folder is the next.js app for **chalk**. see the [repository readme](../README.md) for the full buildathon description, architecture, and setup for both `frontend/` and `backend/`.

quick start: copy `.env.example` to `.env.local`, set `NEXT_PUBLIC_API_URL`, then `npm install` and `npm run dev`.
